<Window x:Class="hw5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:hw5"
        mc:Ignorable="d"
        Title="Панель настроек" Height="250" Width="250">
    <Window.Resources>
        <Style x:Key="InteractiveButtonStyle" BasedOn="{StaticResource BaseButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource HoverColor}"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="{StaticResource PressedColor}"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsMouseOver" Value="True"/>
                        <Condition Property="IsEnabled" Value="False"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="Background" Value="{StaticResource DisabledColor}"/>
                </MultiTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <StackPanel>
        <TextBlock Text="Настройки приложения" Style="{StaticResource HeaderTextStyle}" HorizontalAlignment="Center"/>
        <Button Content="Сохранить" Style="{StaticResource InteractiveButtonStyle}"/>
        <Button Content="Отменить" Style="{StaticResource InteractiveButtonStyle}"/>
        <CheckBox x:Name="ToggleButton" Content="Включить режим" Margin="10,20,10,0"/>
        <Button Content="Специальная кнопка"
           Style="{StaticResource InteractiveButtonStyle}"
           IsEnabled="{Binding IsChecked, ElementName=ToggleButton}"/>
    </StackPanel>
</Window>
